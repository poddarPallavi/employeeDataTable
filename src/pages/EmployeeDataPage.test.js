import { render } from "@testing-library/react";
import { EmployeeDataPage } from "./EmployeeDataPage";

describe("renders Employee data page", () => {
  test("renders Employee data page component", () => {
    // render(<EmployeeDataPage />);
  });
});
